flask>=3.0
gunicorn>=21.2
requests>=2.32
aiohttp>=3.9
Pillow>=10.0
python-docx>=1.1.0

# Azure AI Foundry agents
azure-ai-agents>=1.0.0
# Pinned to 2.x: the new Foundry Conversations + Responses API (v2 agent path,
# used by Script-Shorten-Agent and others) is only available in azure-ai-projects
# >= 2.0. Older 1.x returns an AzureOpenAI client pinned to an api-version that
# lacks the /openai/conversations endpoint (HTTP 404), breaking v2 agents.
azure-ai-projects==2.1.0
azure-identity>=1.17
azure-storage-blob>=12.20

# OpenAI / Azure OpenAI (used by some agent clients)
openai>=1.40

# xAI Grok SDK — generates unique b-roll video clips (grok-imagine-video) and
# rewrites scene descriptions into theme-specific visual prompts (grok-3-mini).
xai-sdk>=1.11

# Anthropic Claude — preferred prompt engineer for grok-imagine-video prompts
# (Opus 4.8). Requires ANTHROPIC_API_KEY in the container env; if absent, the
# Grok prompt step falls back to the xAI grok-3-mini rewrite above.
anthropic>=0.69

# AutoGen (used by enhanced multi-agent system)
autogen-agentchat>=0.4.0
autogen-core>=0.4.0

# X (Twitter) posting for @AIwithRoz promos/standalone posts
tweepy>=4.14.0
